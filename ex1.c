#include<stdio.h>
float sum (float[],int);
float inpuy [MAX_SIZE],answer;
int i;
main(){
  for(i=0; i<MAX_SIZE;i++);
  a[i],i;
  answer=sum(a,MAX_SIZE);
  printf("sum is :%f\n",answer);
}
float sum(float list[],int n)
{
  int i;
  float tempsum=0;
  for(i=0;i<n;i++){
  tempsum+=list[];
  return tempsum;
}
}


